/**
 * concepts-sprint3.ts
 * Sprint 3 new concepts — append to CONCEPTS in concepts.ts
 */
import { Concept } from './concepts';

export const SPRINT3_CONCEPTS: Concept[] = [
  {
    slug: 'database-sharding-advanced',
    title: 'Database Sharding (Advanced)',
    summary: 'Range, hash, and directory-based sharding with virtual nodes and resharding strategies.',
    deepDive: 'Range sharding partitions by key range — simple but creates hotspots at the high end. Hash sharding distributes evenly but makes range queries expensive. Directory-based sharding uses a lookup table for maximum flexibility but adds a network hop. Virtual nodes (vnodes) allow resharding by remapping token ranges without full data migration.',
    emoji: '🔀',
    difficulty: 'advanced',
    relatedMissions: ['shard-or-die', 'youtube-deep-read'],
    relatedPaths: ['high-read', 'scale-streaming'],
  },
  {
    slug: 'federated-architecture',
    title: 'Federated Architecture & AT Protocol',
    summary: 'Decentralised systems where users own their data across independent servers.',
    deepDive: 'In federated architectures (ActivityPub, AT Protocol), each user owns their data on their own server (PDS). A global relay aggregates events into a single stream (Firehose). AppViews subscribe to the Firehose to build user-facing products. DIDs provide user-owned identity portable across servers. Cross-server timelines are eventually consistent.',
    emoji: '🌐',
    difficulty: 'advanced',
    relatedMissions: ['how-bluesky-works'],
    relatedPaths: ['real-time'],
  },
  {
    slug: 'circuit-breaker-pattern',
    title: 'Circuit Breaker Pattern',
    summary: 'Stop calling failing services and return fallbacks to prevent cascade failures.',
    deepDive: 'Circuit breaker states: CLOSED (normal), OPEN (block calls, return fallback), HALF-OPEN (send one probe). Trips when error rate exceeds threshold (e.g. 50% in 10s). Bulkhead pattern isolates service thread pools. Resilience4J, Hystrix, and Istio implement this. Timeout + circuit breaker together: timeout prevents slow threads; circuit breaker prevents retrying a broken service.',
    emoji: '⚡',
    difficulty: 'advanced',
    relatedMissions: ['circuit-breaker'],
    relatedPaths: ['scale-streaming', 'consistency'],
  },
  {
    slug: 'pubsub-fan-out',
    title: 'Pub/Sub & Fan-Out Patterns',
    summary: 'Broadcasting events from one producer to many consumers via message broker.',
    deepDive: 'Pub/Sub decouples event producers from consumers. Fan-out on write: pre-compute per recipient on publish (fast reads, expensive writes). Fan-out on read: compute at read time (cheap writes, slower reads). Hybrid: fan-out on write for regular users, fan-out on read for mega-celebrities. Sticky sessions required for WebSocket fan-out.',
    emoji: '📢',
    difficulty: 'intermediate',
    relatedMissions: ['sports-leaderboard', 'notification-engine', 'presence-at-scale'],
    relatedPaths: ['real-time', 'async-queues'],
  },
  {
    slug: 'graphql-architecture',
    title: 'GraphQL Architecture & DataLoader',
    summary: 'Query language eliminating over-fetching and N+1 problems with batch resolvers.',
    deepDive: 'GraphQL lets clients specify exactly the fields they need. DataLoader batches individual entity fetches into bulk queries, eliminating N+1. Persisted queries (hash instead of query text) enable CDN caching. Federation (Apollo Federation) splits schema across microservices. Normalised cache by entity ID enables efficient cache invalidation.',
    emoji: '🔗',
    difficulty: 'intermediate',
    relatedMissions: ['rest-vs-graphql'],
    relatedPaths: ['foundations'],
  },
  {
    slug: 'event-sourcing',
    title: 'Event Sourcing & Outbox Pattern',
    summary: 'Store state as an immutable event log; derive current state by replay.',
    deepDive: 'Event sourcing: state = replay of all events. Benefits: full audit trail, time-travel queries, event replay for debugging. Outbox pattern: write event to local DB table atomically with business operation, then relay publishes to queue — prevents lost events on crash. Event schema versioning is critical for long-running systems.',
    emoji: '📜',
    difficulty: 'advanced',
    relatedMissions: ['event-driven-microservice', 'how-bluesky-works'],
    relatedPaths: ['async-queues', 'consistency'],
  },
  {
    slug: 'distributed-locks-advanced',
    title: 'Distributed Locks & Fencing Tokens',
    summary: 'Redis Redlock quorum, ZooKeeper ephemeral nodes, and fencing tokens for safe exclusion.',
    deepDive: 'Redlock acquires a lock by writing to N independent Redis nodes and succeeding only on majority quorum (N/2+1) within clock-drift tolerance. Fencing tokens are monotonically increasing counters — storage checks the token and rejects writes from stale holders. ZooKeeper ephemeral nodes auto-delete on session loss, providing stronger guarantees than TTL-based Redis locks.',
    emoji: '🔐',
    difficulty: 'advanced',
    relatedMissions: ['distributed-locks-deep-dive', 'sports-leaderboard'],
    relatedPaths: ['consistency', 'real-time'],
  },
  {
    slug: 'two-phase-commit-advanced',
    title: 'Two-Phase Commit & Saga Pattern',
    summary: 'Coordinating atomic cross-service transactions with 2PC or compensating Saga actions.',
    deepDive: '2PC Phase 1: coordinator asks all participants to PREPARE (lock resources and vote). Phase 2: all YES → COMMIT, any NO → ROLLBACK. Key weakness: blocking — participants hold locks until Phase 2. Coordinator crash = indefinite lock hold. Saga: each service does a local transaction and publishes an event. On failure, compensating transactions undo. Saga is non-blocking; 2PC provides stronger atomicity.',
    emoji: '🤝',
    difficulty: 'advanced',
    relatedMissions: ['two-phase-commit-practice', 'event-driven-microservice'],
    relatedPaths: ['high-read', 'consistency'],
  },
  {
    slug: 'observability-platform',
    title: 'Observability Platform Design',
    summary: 'Metrics ingestion pipelines, time-series databases, and alert evaluation at scale.',
    deepDive: 'Three pillars: Metrics (numbers over time), Logs (text events), Traces (distributed request flows). TSDB stores data columnar by metric+labels with compression. Downsampling: raw 7 days, 1-min rollups 3 months, hourly 15 months — 100x storage reduction. Alert evaluation runs every 30s across thousands of rules. Cardinality explosion (millions of unique label combinations) is the #1 TSDB killer.',
    emoji: '📊',
    difficulty: 'advanced',
    relatedMissions: ['observability-at-scale', 'full-stack-observability-capstone'],
    relatedPaths: ['scale-streaming'],
  },
  {
    slug: 'realtime-game-architecture',
    title: 'Real-Time Game Server Architecture',
    summary: 'Authoritative servers, tick rates, dead reckoning, and session state management.',
    deepDive: 'Game servers are stateful: all players in a session must connect to the same server (consistent-hash LB). Tick rate (50ms) drives state sync: server processes inputs, simulates physics, broadcasts state delta. Authoritative server model: server is source of truth — clients predict locally but server reconciles. Dead reckoning extrapolates missing positions from last velocity. Cache snapshots enable crash recovery.',
    emoji: '🎮',
    difficulty: 'advanced',
    relatedMissions: ['multiplayer-game-server'],
    relatedPaths: ['real-time'],
  },
  {
    slug: 'concurrency-models',
    title: 'Concurrency Models (Async vs Parallel)',
    summary: 'Thread pools, event loops, goroutines — choosing the right model for I/O vs CPU tasks.',
    deepDive: 'I/O-bound concurrency: event loops (Node.js, Python asyncio) — single thread handles thousands of concurrent connections with minimal memory. CPU-bound parallelism: os.cpu_count() worker processes — more threads = context switching overhead. Go goroutines: lightweight cooperative threads. Bounded work queues implement backpressure — reject tasks when full instead of OOM.',
    emoji: '⚙️',
    difficulty: 'intermediate',
    relatedMissions: ['concurrency-vs-parallelism'],
    relatedPaths: ['async-queues'],
  },
  {
    slug: 'presence-heartbeat',
    title: 'Presence Systems & Heartbeat Design',
    summary: 'TTL-based presence detection, fan-out to contacts, and thundering herd prevention.',
    deepDive: 'Redis TTL-based presence: SET user:{id}:online EX 30 — key auto-expires if no heartbeat in 30s. Heartbeat interval = TTL/2. Fan-out on status change: publish to channel per user — only online contacts subscribed. Thundering herd on reconnect: add jitter (random 0–5s delay) to client reconnect timers. Multi-device presence: user is online if ANY device has sent a heartbeat in the last 30s.',
    emoji: '💚',
    difficulty: 'intermediate',
    relatedMissions: ['presence-at-scale'],
    relatedPaths: ['real-time'],
  },
];
